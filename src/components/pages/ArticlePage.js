import React from 'react';
import ArticleDescription from '../ArticleDescription'

const ArticlePage = ({ itemId }) => {
    return (
        <ArticleDescription itemId={ itemId }/>
    );
};

export default ArticlePage; 